# My Pomodoro Timer

A clean Pomodoro timer app built with React and Vite.

## Setup Instructions

1. Extract all files to `~/code/pomodoro/` (or wherever you want)

2. Install dependencies:
```bash
cd ~/code/pomodoro
npm install
```

3. Start the development server:
```bash
npm run dev
```

4. Open http://localhost:5173 in your browser

## Project Structure

```
pomodoro-app/
├── index.html          # Entry HTML file
├── package.json        # Dependencies
├── vite.config.js      # Vite configuration
└── src/
    ├── main.jsx        # React entry point
    ├── index.css       # Global styles
    ├── App.jsx         # Main app component
    └── App.css         # App styles
```

## Features (Current)

- Dark/Light theme toggle
- Basic timer display (25:00)
- Responsive layout

## Next Steps

You can now:
1. Add actual timer countdown functionality
2. Implement session tracking
3. Build statistics dashboard
4. Add CSV import/export
5. Customize colors and styling
